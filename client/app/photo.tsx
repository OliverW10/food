import React from "react";
import { Text } from "react-native";

export default function PhotoPage(){
    return <>
        <Text>Page to take a photo, may not be required if we open the camera directly</Text>
    </>
}